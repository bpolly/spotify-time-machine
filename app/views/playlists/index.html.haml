.container
  %table.table.is-fullwidth
    - @playlists.each do |playlist|
      = link_to playlist_path(playlist) do
        %section.hero{ class: "is-#{hero_color}"}
          .hero-body
            .container
              %h1.title= playlist.name
